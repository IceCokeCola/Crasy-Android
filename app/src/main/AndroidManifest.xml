<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.xiaolong.crasyandroid" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".CrasyAndroidActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".activities.DevelopEntertainmentActivity"
            android:label="Develop Entertainment" >
        </activity>

        <activity android:name=".firstchapter.HelloWorldActivityList"
            android:label="Hello World" >

        </activity>

        <activity android:name=".firstchapter.HelloWorldList"
            android:label="Hello World" >
        </activity>

        <activity
            android:name=".firstchapter.HelloWorldActivity"
            android:label="@string/title_activity_hello_world">
        </activity>

        <activity android:name=".firstchapter.HelloWorld"
            android:label="Hello World" >
        </activity>

        <activity android:name=".activities.ChapterActivity" ></activity>
        <activity android:name=".activities.SectionListActivity" ></activity>
    </application>

</manifest>
